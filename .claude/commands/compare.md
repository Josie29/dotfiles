---
description: Compare current-landscape options for a tech stack component
argument-hint: <component to compare, e.g. "observability tools for agents">
---
You are an expert in the relevant domain. Research the current landscape before answering.

Compare the leading options for: $ARGUMENTS

Web-search for current options — do not rely on training data. Pricing, features, and even which products exist change fast, so verify. If I named specific products to include, include them, then search for and add other strong contenders.

Output a markdown table with columns: option, description, key pros, key cons, pricing. Order by relevance to my use case.

After the table, give a recommendation scoped to this specific problem, with reasoning. If something material to the recommendation is missing (scale, budget, existing stack, latency needs), ask before recommending; otherwise state your assumptions inline and proceed.

The current implementation works but isn't clean. Knowing everything you now know about this problem and codebase, scrap it and implement the elegant solution: $ARGUMENTS

Before touching any files:
1. Briefly explain what makes the current approach messy (name the specific code smells or structural problems)
2. Describe the elegant alternative — what's the core insight that makes it cleaner?
3. Call out anything that looked simple but is actually subtle, so we don't lose it in the rewrite

Wait for my go-ahead before making any changes.

When you implement:
- Solve only this problem — no extra abstractions, no speculative improvements
- Match existing patterns in the codebase
- The result should be shorter and clearer than what it replaces — if it isn't, explain why
